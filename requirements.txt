# Pip-managed Python dependencies for local tools and vision nodes.
# ROS2 packages (rclpy, cv_bridge, sensor_msgs, nav2_msgs, etc.) are installed via ROS, not pip.

openai>=1.0.0
ultralytics>=8.0.0
opencv-python>=4.8.0
